app:
  data:
    inject: false
  services:
    items: http://localhost:12345

eureka:
  client:
    enabled: false
ribbon:
  eureka:
    enabled: false

logging:
  level:
    org:
      springframework:
        security: DEBUG
